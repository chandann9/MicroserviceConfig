spring:
  application:
    name:  ACCOUNT-SERVICEhjgsdjf
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8066/eureka
